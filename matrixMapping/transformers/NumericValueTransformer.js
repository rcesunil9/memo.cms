import React from "react"

const NumericValueTransformer = ({value, onChange}) => (
  <React.Fragment>
    <i>No transformation available.</i>
  </React.Fragment>
)

export default NumericValueTransformer
